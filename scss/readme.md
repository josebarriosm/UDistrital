# SASS

Este es el proyecto SASS

## Ejecución

Para ejecutar este proyecto se deben tener en cuenta dos comandos:

- Ejecutar la extensión _Live Server_ de VSCode
- SASS

sass --watch scss/estilo.css css/estilo.css

El flag --watch permite monitorear los cambios en los archivos **scss** y generar automáticamente código **css**
